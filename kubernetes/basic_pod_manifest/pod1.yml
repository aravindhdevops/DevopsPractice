kind: Pod
apiVersion: v1
metadata:
  name: testpod1
  annotations:
    description: my first pod
spec:
  containers:
    - name: c01
      image: ubuntu
      command: ["/bin/bash", "-c", "while true; do echo 'aravivo-SE';sleep 3; done"]
      env:
        - name: myname
          value: aravivo-se